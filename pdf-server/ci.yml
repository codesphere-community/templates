prepare:
  steps:
    - name: Setting up the permission.
      command: chmod +x setup.sh
    - name: Installing the dependencies.
      command: ./setup.sh
test:
  steps: []
run:
  steps:
    - name: Booting up the Stirling-PDF.
      command: cd /home/user/app/Stirling-PDF && ./gradlew bootRun
